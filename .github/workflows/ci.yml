name: CI
on:
  push:
  pull_request:
  schedule:
    - cron: 13 01 * * *

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker build -t fluree/crypto .
      - name: Run CLJ tests
        run: docker run --rm fluree/crypto lein test
      - name: Run CLJS tests
        run: docker run --rm fluree/crypto npm run ci-cljs